apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: webgui
  namespace: datapower
spec:
  host: webgui-dp.${OCP_DOMAIN}
  port:
    targetPort: 9090
  tls:
    termination: passthrough
  to:
    kind: Service
    name: datapower-svc
    weight: 100
  wildcardPolicy: None